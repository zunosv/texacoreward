# Run Project

## Install dependencies

```bash
npm install --legacy-peer-deps

If dependency errors continue:

npm install --force
Start development server
npm run dev
Open in browser
http://localhost:3000
